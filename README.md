Please install (type this in terminal) before you run the app:
    pip install PySide6

if the code above doesn't work, use this instead:
    C:\Users\ADMIN\AppData\Local\Programs\Python\Python314\python.exe -m pip install PySide6